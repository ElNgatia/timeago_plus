name: timeago_plus_flutter
description: Timeago Widget for Flutter to create fuzzy timestamps. (e.g. "15 minutes ago").
version: 1.1.0
homepage: https://github.com/ElNgatia/timeago_plus

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  timeago_plus: ^1.3.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter


