name: timeago_plus
description: >
  A library useful for creating fuzzy timestamps. (e.g. "15 minutes ago")
version: 1.3.0
homepage: https://github.com/ElNgatia/timeago_plus

environment:
  sdk: '>=2.12.0 <4.0.0'

dependencies:
  intl: ">=0.18.0 <0.21.0"

dev_dependencies:
  test: ^1.21.4
  build_runner: ^2.2.0
  build_web_compilers: ^4.0.6
